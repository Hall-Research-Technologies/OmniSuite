Firmware binaries are intentionally not tracked in git.

Reason:
- GitHub rejects files larger than 100 MB.

Usage:
- Place required .vpup2 files in this folder locally before running the app or local builds.
- GitHub release workflows build application artifacts; firmware packages are managed separately.
